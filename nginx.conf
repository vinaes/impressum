server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/html text/css text/plain application/javascript application/xml image/svg+xml;

    # html не кэшируем, чтобы обновления подхватывались сразу
    location = /index.html {
        add_header Cache-Control "no-cache";
    }

    location = /og.jpg {
        add_header Cache-Control "public, max-age=86400";
    }

    location ~ ^/(robots\.txt|sitemap\.xml|llms\.txt)$ {
        add_header Cache-Control "public, max-age=3600";
    }

    location / {
        try_files $uri /index.html;
    }
}
